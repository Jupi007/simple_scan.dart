name: libsane
description: Dart bindings for the SANE library
version: 0.1.0
repository: https://github.com/ubuntu-flutter-community/simple_scan.dart

platforms:
  linux:

environment:
  sdk: ^3.5.0

dependencies:
  ffi: ^2.2.0
  logging: ^1.3.0
  meta: ^1.17.0
  simple_scan_query_bus: ^0.1.0

dev_dependencies:
  ffigen: ^20.1.1
  flutter_lints: ^6.0.0
  mocktail: ^1.0.5
  test: ^1.31.0
